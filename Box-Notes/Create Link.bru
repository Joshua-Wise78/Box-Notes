meta {
  name: Create Link
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/links/
  body: json
  auth: inherit
}

body:json {
  {
    "source_note_id": "{{noteA_id}}",
    "target_note_id": "{{noteB_id}}",
    "link_type": "reference"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
